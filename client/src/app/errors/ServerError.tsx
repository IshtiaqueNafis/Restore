import {Container, Paper} from "@mui/material";

const ServerError = () => {
    return (
        <Container component={Paper}>
            <h1>server error</h1>
        </Container>
    );
};

export default ServerError;